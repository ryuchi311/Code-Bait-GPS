# Example .env file
# Copy this to .env and fill in your keys. Do NOT commit .env to source control.

# Flask session secret key
FLASK_SECRET_KEY=replace-with-your-secret

# Optional Google Maps API key for embedding the JS Maps API
GOOGLE_MAPS_API_KEY=
